[Unit]
Description=telvm node agent
After=docker.service
Requires=docker.service

[Service]
ExecStart=/usr/local/bin/telvm-node-agent --port 9100 --token ${TELVM_NODE_TOKEN}
EnvironmentFile=-/etc/telvm-node-agent.env
Restart=on-failure
RestartSec=5

[Install]
WantedBy=multi-user.target
